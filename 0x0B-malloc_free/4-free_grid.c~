#include <stdlib.h>
#include "holberton.h"

/**
 * 
 * @
 *
 * Return: 
 */
void free_grid(int **grid, int height)
{
	int i = 0;
	for (; i < height; i++)
	{
		printf("*: |%lu|\n",(unsigned long)*(grid+i));
		free(grid[i]);
	}
	
}
